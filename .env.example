# Copy to .env on voidberry and fill in real values — never commit .env
GEMINI_API_KEY=
GROQ_API_KEY=
ELEVENLABS_API_KEY=
MEM0_API_KEY=
OWM_API_KEY=
# Pi-hole v6: use the plaintext password you log into the admin panel with
# (or generate a dedicated app password at Settings → API → App passwords)
PIHOLE_PASSWORD=
PIHOLE_HOST=192.168.1.103
PIHOLE_PORT=8080
